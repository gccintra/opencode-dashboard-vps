name: Deploy to VPS

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: prod
    steps:
      - name: Pull latest changes
        working-directory: /root/production/opencode-dashboard-vps
        run: git pull origin main
      - name: Install & Build
        working-directory: /root/production/opencode-dashboard-vps
        run: |
          bun install --frozen-lockfile
          bun run build
      - name: Restart production
        run: pm2 restart opencode-dashboard
