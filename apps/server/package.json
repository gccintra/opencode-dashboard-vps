{
  "name": "@opencode/server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "NODE_ENV=development DATABASE_PATH=./data/opencode.db SERVER_PORT=3002 bun run --watch --env-file ../../.env src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun && cp src/db/schema.sql dist/schema.sql",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/",
    "test": "vitest run"
  },
  "dependencies": {
    "elysia": "^1.3.0",
    "jose": "^6.2.3"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/bun": "^1.3.2",
    "better-sqlite3": "^12.10.0",
    "eslint": "^9.30.1",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.36.0",
    "vitest": "3.2.6"
  }
}
